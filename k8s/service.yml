apiVersion: v1
kind: Service
metadata:
  name: fire
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fire"
    port: 8080
  selector:
    app: fire
